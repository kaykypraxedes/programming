#!/bin/sh
# tb_add_duplicate_passenger
# Tenta adicionar passageiro com ID já existente — deve rejeitar

. "$(dirname "$0")/helpers.sh"

set_input "3\n1\n1\n5\n"

assert_contains "ja foi cadastrado" || exit 1
