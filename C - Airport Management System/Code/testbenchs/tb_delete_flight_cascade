#!/bin/sh
# tb_delete_flight_cascade
# Remove voo 1 e verifica remoção em cascata de passagens e passageiros

. "$(dirname "$0")/helpers.sh"

set_input "1\n3\n1\n4\n5\n"

assert_not_contains "Voo 1"       || exit 1
assert_contains "Voo 2"          || exit 1
assert_not_contains "Joao Silva" || exit 1
assert_contains "Kayky Praxedes" || exit 1
